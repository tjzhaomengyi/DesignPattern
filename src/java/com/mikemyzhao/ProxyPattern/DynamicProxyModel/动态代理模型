1、动态代理和静态代理的区别
都是需要实现一个代理类，you区别，注意看父类，动态代理的主要意图就是解决我们常说的"审计"问题，也就是横切面编程，
在不改变我们已有代码结构的情况下增强或者控制对象的行为。
要实现动态代理的首要条件是：被代理类必须实现一个接口。

2、代理模式的使用
Spring AOP和AspectJ。在学些AOP的时候，弄清：切面（Aspect）、切入点（JoinPoint）、通知（Advice)、织入（Weave）就足够了。
理解了这几个名词，应用就可以了。